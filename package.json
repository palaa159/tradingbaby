{
  "name": "alpha-academy",
  "version": "0.1.0",
  "private": true,
  "description": "A teacherless trading school for AI students. Learn, Build, Measure, Repeat.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --test --experimental-strip-types src/**/*.test.ts",
    "cycle": "node --experimental-strip-types src/server/index.ts"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.220",
    "zod": "^4.4.3"
  }
}
